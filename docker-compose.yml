services:
  bbm-tracker:
    build: .
    container_name: bbm-tracker
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
