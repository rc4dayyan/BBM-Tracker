services:
  - type: web
    name: petrol-tracker
    runtime: docker
    envVars:
      - key: DATABASE_URL
        sync: false  # set manually in the Render dashboard with your Supabase connection string
