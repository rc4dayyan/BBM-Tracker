services:
  - type: web
    name: petrol-tracker
    runtime: docker
    envVars:
      - key: SUPABASE_URL
        sync: false  # e.g. https://xxxx.supabase.co
      - key: SUPABASE_KEY
        sync: false  # anon/public key from Supabase API settings
      - key: TZ
        value: Asia/Jakarta
