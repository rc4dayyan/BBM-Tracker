services:
  - type: web
    name: petrol-tracker
    runtime: docker
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: petrol-db
          property: connectionString

databases:
  - name: petrol-db
    databaseName: petrol_tracker
    plan: free
